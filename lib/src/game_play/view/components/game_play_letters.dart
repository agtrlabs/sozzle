import 'package:flutter/material.dart';

class GamePlayLetters extends StatelessWidget {
  const GamePlayLetters({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Letters');
  }
}
