class SettingsHelper {
  static bool? turnOffMuteMaybe({required bool val}) {
    return val ? false : null;
  }

  static bool? turnOffSoundAndMusicMaybe({required bool val}) {
    return val ? false : null;
  }
}
